import {
	BufferGeometry
} from '../../../src/Three';

export class SpiralSphereGeometry extends BufferGeometry {

	constructor( radius: number, turns: number, gapX?: number, gapY?: number, tileX?: number, tileY?: number, tiles?: number );

}
