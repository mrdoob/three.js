#if MAX_LOCAL_FOGS > 0

uniform vec3  localFogPosition[ MAX_LOCAL_FOGS ];
uniform vec3  localFogColor[ MAX_LOCAL_FOGS ];
uniform float localFogRadius[ MAX_LOCAL_FOGS ];
uniform float localFogFar[ MAX_LOCAL_FOGS ];
	
#endif