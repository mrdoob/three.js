/**
 * @author TristanVALCKE / https://github.com/Itee
 */
/* global module, test */

module.todo( 'DataTexture', () => {

	test( 'write me !', assert => {

		assert.ok( false, "everything's gonna be alright" );

	} );

} );
